# ID Registry System  
**ระบบจัดการข้อมูลบุคคลสำหรับงานภาคสนาม / ศูนย์พักพิง / งานลงทะเบียนชั่วคราว**

ระบบนี้ถูกออกแบบเพื่อช่วยให้เจ้าหน้าที่สามารถบันทึกข้อมูลบุคคลได้อย่างรวดเร็ว รองรับเครื่องอ่านบัตรประชาชนและการเพิ่มข้อมูลแบบแมนนวล

---

## 🧭 ระบบประกอบด้วย 2 ส่วนหลัก

### 🌐 Web Application (PHP) สำหรับจัดการข้อมูลฝั่ง UI

### 🪪 Smart Card ID Bridge โปรแกรมอ่านบัตรประชาชน ทำงานผ่าน Local API

---

## 🛠 โครงสร้าง

### Web
- PHP 8+
- MySQL / MariaDB
- Composer
- Bootstrap 5
- jQuery
- jquery.Thailand.js

### Smart Card Bridge
- .NET Windows
- PCSC
- ThaiNationalIDCard Library

---

## 🔐 ความปลอดภัยข้อมูล
- เข้ารหัสข้อมูลด้วย Key จาก `.env`
- Hash เลขบัตรเพื่อใช้ค้นหา
- Password Hash ด้วยมาตรฐาน PHP

---

## 📦 การติดตั้ง

### 1️⃣ เตรียมเซิร์ฟเวอร์ด้วยโปรแกรมจำลอง เช่น Wampserver และสร้างฐานข้อมูล

### 2️⃣ ดาวน์โหลดไฟล์ติดตั้งแล้วคลายซิบไฟล์ออกจะพบโฟลเดอเหล่านี้

- โฟลเดอร์ web ทำการคัดลอกไว้ภายใต้โฟลเดอร์ Root และเปลี่ยนชื่อตามต้องการ เช่น `www\Reg`
- โฟลเดอร์ installer/id-bridge ทำการคัดลอกไว้ตำแหน่งใดก็ได้ 
- ภายในโฟลเดอร์ id-bridge คลิกขวาที่ไฟล์ setup.bat ด้วยสิทธิ์ Admin เพื่อลงทะเบียนโปรแกรม  
- สร้างโฟลเดอร์ config, uploads ภายใต้ www\Reg
- installer/src ไฟล์โปรเจคสำหรับนำไปพัฒนาต่อ

### 3️⃣ ติดตั้ง Library ผ่าน Command Prompt
- เปิด Terminal พิมพ์คำสั่ง cd ตามด้วยที่อยู่โปรเจค เช่น 
`cd C:\wamp64\www\Reg`
- รันคำสั่ง
 `composer install`

### 4️⃣ เข้าสู่หน้าติดตั้ง `http://localhost/ชื่อโปรเจค/install/` แล้วดำเนินการติดตั้งให้สมบูรณ์

---

## 📦 การย้ายเซิร์ฟเวอร์
1. สำรองข้อมูลจากเครื่องต้นทาง
- ฐานข้อมูล
- ไฟล์โปรเจกต์ (สามารถข้ามโฟลเดอร์ vendor และติดตั้ง Composer ผ่าน Command Prompt ได้)
- รูปภาพ โฟลเดอร์ uploads
2. เตรียมเซิร์ฟเวอร์ปลายทาง
- ติดตั้งโปรแกรม WAMP หรือ XAMPP
- สร้างฐานข้อมูลและนำเข้าฐานข้อมูลที่เตรียมไว้
- คัดลอกไฟล์โปรเจกต์มาไว้ที่โฟลเดอร์ปลายทาง
- แก้ไขการเชื่อมต่อฐานข้อมูลที่ไฟล์ .env
### บางครั้งจะพบปัญหาจากไลบรารี จึงควรติดตั้งใหม่ผ่านคำสั่ง `composer install` ใน Command Prompt
3. ตั้งค่า Network
- เพื่อให้ QR Code สามารถใช้งานได้ปกติให้ตั้งค่า IP เป็น Static IP
- ตั้งค่า Firewall เปิด Port 80 หรือ 8080
- ภายในโฟลเดอร์ id-bridge คลิกขวาที่ไฟล์ setup.bat ด้วยสิทธิ์ Admin เพื่อลงทะเบียนโปรแกรม

---

## การเตรียมเครื่องอ่านบัตร

1. ทำการเชื่อมต่อเครื่องอ่านบัตรประชาชนเข้ากับคอมพิวเตอร์
2. ติดตั้งไดรเวอร์ให้ตรงรุ่นพร้อมตรวจสอบ
3. บางกรณีอาจพบปัญหา Registry หรือ Firewall

---

## การเพิ่มข้อมูล
1. คลิกที่ปุ่ม "ลงทะเบียนใหม่" เพื่อเข้าสู่หน้าเพิ่มข้อมูลแบบแมนนวล
2. คลิก "อ่านข้อมูลจากบัตรประชาชน" รอสักพัก (10-20 วินาที) ระบบจะดึงข้อมูลมาแสดงอัตโนมัติ
3. ทำการสอบถามข้อมูลเพิ่มเติมเช่น เบอร์ติดต่อ, อาการเจ็บป่วย
4. ด้านล่างสุดกดปุ่ม "ส่งข้อมูลไปที่แท็บเล็ต"
- กรณีทดสอบให้เปิดหน้า http://localhost/ชื่อโปรเจค/index.php?page=guest_display
5. การเชื่อมหน้าจอแท็บเบล็ตเพื่อให้ผู้เข้าพักกดยินยอม
- ทั้งคอมพิวเตอร์และแท็บเล็ตต้องต่อ Wi-Fi หรือ LAN วงเดียวกัน
- ตั้งค่า Firewall เปิดพอร์ต (ส่วนใหญ่คือพอร์ต 80) เพื่ออนุญาตให้แท็บเล็ต "มองเห็น" เว็บเซิร์ฟเวอร์ในคอมพิวเตอร์
- ตั้งค่า Static IP เพื่อป้องกัน Router เปลี่ยนเลข IP เครื่องคอมฯ
- ที่หน้าจอแท็บเบล็ตเข้าผ่านเลข IP เช่น http://เลข ip/ชื่อโปรเจค/pages/guest_display.php
- เมื่อผู้เข้าพักกดยินยอม ระบบจะกดเช็คบ็อกให้อัตโนมัติ ให้เรากดเช็คบ็อกการตรวจทานและบันทึกข้อมูล
- (สามารถสร้าง Qr code แล้วปล่อยฟรีไวไฟให้ผู้พักเชื่อมต่อและสแกนลิงค์ยืนยันข้อมูลด้วยตนเองได้)
- ปัญหา "You don't have permission" คลิกซ้ายที่ไอคอน WAMP (มุมล่างขวา) เลือก Apache > httpd-vhosts.conf

```
# Virtual Hosts
#
<VirtualHost _default_:80>
  ServerName localhost
  ServerAlias localhost
  DocumentRoot "${INSTALL_DIR}/www"
  <Directory "${INSTALL_DIR}/www/">
    Options +Indexes +Includes +FollowSymLinks +MultiViews
    AllowOverride All
    # เปลี่ยนจาก Require local เป็นบรรทัดล่างนี้
    Require all granted
    Require local
  </Directory>
</VirtualHost>
```
- Save ไฟล์ และทำการ Restart All Services ของ WAMP

6. ระบบจะทำการบันทึกข้อมูลผู้เข้าพัก
- กรณีไม่มีข้อมูลเก่า ระบบจะบันทึกแล้วส่งไปยังหน้าประวัติ
- กรณีมีข้อมูลเก่า ระบบจะแสดงหน้าจอเปรียบเทียบและตรวจสอบสถานะการพัก เพื่อให้ผู้ดูแลตัดสินใจ
7. การแจ้งออกจากที่พัก ในหน้าประวัติส่วนบุคคล แถวล่างสุดจะมีปุ่มแจ้งออก เมื่อกดจะบันทึกเวลาไว้เป็นสถิติ

---

## 📜 License

This project is licensed under the GNU GPL v3 License.
See LICENSE file for details.

---


